# chatty
An intelligent chatbot built using Retrieval-Augmented Generation (RAG) that combines semantic search and large language models to answer user queries from custom datasets. The system retrieves relevant documents, provides context to the LLM, and generates accurate, grounded responses.
